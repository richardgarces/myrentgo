# =============================================================================
# Node Exporter — métricas del host (CPU, disco, red)
# Copia: cp .env.example .env && chmod 600 .env
# =============================================================================

APP_ID=platform

# NODE_EXPORTER_PORT: scrape en localhost. Prometheus usa platform-node-exporter:9100
# NO abras 9100 a Internet.
NODE_EXPORTER_PORT=9100
