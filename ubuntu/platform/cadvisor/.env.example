# =============================================================================
# cAdvisor — métricas de contenedores Docker
# Copia: cp .env.example .env && chmod 600 .env
# =============================================================================

APP_ID=platform

# CADVISOR_PORT: UI/métricas en localhost. Interno: platform-cadvisor:8080
# NO abras este puerto a Internet.
CADVISOR_PORT=8088
